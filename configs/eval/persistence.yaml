method: persistence

data:
  wanted_domains: [cancer, hiv, warfarin, mimic]
  ready_inputs:
    ready_dirs:
      - outputs/pfn_ready

evaluation:
  output_dir: outputs/eval/persistence

baseline:
  {}
